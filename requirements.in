# Add your integration-specific dependencies here
backoff

# Generic-model delivery (overrides base pins during rollout; switch to a
# published version once gundi-core 1.11.3 / gundi-client-v2 3.2.0 are cut).
gundi-core @ git+https://github.com/PADAS/gundi-core.git@feat/add-gundi-delivery
gundi-client-v2 @ git+https://github.com/PADAS/gundi-client.git@feat/add-apply-transformations
